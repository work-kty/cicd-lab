apiVersion: apps/v1
kind: Deployment
metadata:
  name: was-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: was
  template:
    metadata:
      labels:
        app: was
    spec:
      containers:
        - name: was
          image: ghcr.io/work-kty/cicd-was:c10f20f003d86fce0b2d7451ed906abb891c12c4
#          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          envFrom:                      # ConfigMap/Secret의 모든 키를 환경변수로 통째 주입
            - configMapRef:
                name: db-config
            - secretRef:
                name: db-secret