apiVersion: apps/v1
kind: Deployment
metadata:
  name: was-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: was
  template:
    metadata:
      labels:
        app: was
    spec:
      containers:
        - name: was
          image: ghcr.io/work-kty/cicd-was:3efa18f3b8c812f8dd27dcf1d2821cd54074ea53
#          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          envFrom:                      # ConfigMap/Secret의 모든 키를 환경변수로 통째 주입
            - configMapRef:
                name: db-config
            - secretRef:
                name: db-secret