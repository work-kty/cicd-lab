apiVersion: apps/v1
kind: Deployment
metadata:
  name: was-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: was
  template:
    metadata:
      labels:
        app: was
    spec:
      containers:
        - name: was
          image: ghcr.io/work-kty/cicd-was:a3b92441bd9d10e96e76f8cd48d515dd2c038d9f
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          envFrom:                      # ConfigMap/Secret의 모든 키를 환경변수로 통째 주입
            - configMapRef:
                name: db-config
            - secretRef:
                name: db-secret